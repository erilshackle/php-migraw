{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Migraw","text":"<p>SQL-first migrations for PHP. Write SQL. Not magic.</p> <p>Migraw is a lightweight, framework-agnostic database migration tool for PHP.</p> <p>It keeps SQL visible while providing the migration workflow expected from a modern migration system.</p>"},{"location":"#install","title":"Install","text":"<pre><code>composer require eril/migraw\n</code></pre> <p>Initialize Migraw:</p> <pre><code>php vendor/bin/migraw init\n</code></pre> <p>Create a migration:</p> <pre><code>php vendor/bin/migraw make create_users_table\n</code></pre> <p>Run migrations:</p> <pre><code>php vendor/bin/migraw migrate\n</code></pre>"},{"location":"#what-migraw-provides","title":"What Migraw provides","text":"<ul> <li>Raw SQL migrations</li> <li>Fluent SQL helpers</li> <li>Smart migration templates</li> <li>Population migrations</li> <li>Migration batches and rollback</li> <li>Dry-run mode</li> <li>Migration checksums</li> <li>Modified migration detection</li> <li>Repair tools</li> <li>Schema squash and unsquash</li> <li>PDO, callable and array-based connections</li> <li>MySQL, MariaDB, PostgreSQL and SQLite support</li> </ul> <p>Start with Getting Started.</p>"},{"location":"configuration/","title":"Configuration","text":"<p>Migraw uses a <code>migraw.php</code> configuration file in the project root.</p> <p>A typical configuration is:</p> <pre><code>&lt;?php\n\nreturn [\n    // 'bootstrap' =&gt; 'bootstrap.php',\n\n    'path' =&gt; 'database/migrations',\n\n    'template' =&gt; 'raw',\n\n    'connection' =&gt; [\n        'driver' =&gt; $_ENV['DB_CONNECTION'] ?? 'mysql',\n\n        'host' =&gt; $_ENV['DB_HOST'] ?? '127.0.0.1',\n        'port' =&gt; $_ENV['DB_PORT'] ?? '3306',\n        'database' =&gt; $_ENV['DB_DATABASE'] ?? '',\n        'username' =&gt; $_ENV['DB_USERNAME'] ?? 'root',\n        'password' =&gt; $_ENV['DB_PASSWORD'] ?? '',\n        'charset' =&gt; $_ENV['DB_CHARSET'] ?? 'utf8mb4',\n\n        'sqlite_path' =&gt; $_ENV['DB_SQLITE_PATH'] ?? 'database/database.sqlite',\n    ],\n];\n</code></pre>"},{"location":"configuration/#migration-path","title":"Migration path","text":"<pre><code>'path' =&gt; 'database/migrations',\n</code></pre> <p>Defines where Migraw reads and creates migrations.</p>"},{"location":"configuration/#default-template","title":"Default template","text":"<pre><code>'template' =&gt; 'raw',\n</code></pre> <p>Supported values:</p> <pre><code>raw\nfluent\n</code></pre> <p><code>raw</code> keeps generated migrations SQL-first.</p> <p><code>fluent</code> generates migrations using Migraw's fluent SQL helpers.</p>"},{"location":"configuration/#bootstrap","title":"Bootstrap","text":"<p>If your application must be initialized before resolving the database connection:</p> <pre><code>'bootstrap' =&gt; 'bootstrap.php',\n</code></pre> <p>This is useful for loading environment variables, application containers, helpers, or an existing database layer.</p>"},{"location":"configuration/#connection-array","title":"Connection array","text":"<p>MySQL/MariaDB:</p> <pre><code>'connection' =&gt; [\n    'driver' =&gt; 'mysql',\n    'host' =&gt; '127.0.0.1',\n    'port' =&gt; '3306',\n    'database' =&gt; 'app',\n    'username' =&gt; 'root',\n    'password' =&gt; '',\n    'charset' =&gt; 'utf8mb4',\n],\n</code></pre> <p>PostgreSQL:</p> <pre><code>'connection' =&gt; [\n    'driver' =&gt; 'pgsql',\n    'host' =&gt; '127.0.0.1',\n    'port' =&gt; '5432',\n    'database' =&gt; 'app',\n    'username' =&gt; 'postgres',\n    'password' =&gt; '',\n    'charset' =&gt; 'utf8',\n],\n</code></pre> <p>SQLite:</p> <pre><code>'connection' =&gt; [\n    'driver' =&gt; 'sqlite',\n    'host' =&gt; '127.0.0.1',\n    'port' =&gt; '3306',\n    'database' =&gt; '',\n    'username' =&gt; 'root',\n    'password' =&gt; '',\n    'charset' =&gt; 'utf8mb4',\n    'sqlite_path' =&gt; 'database/database.sqlite',\n],\n</code></pre> <p>Only fields required by the selected driver are used.</p>"},{"location":"configuration/#existing-pdo-connection","title":"Existing PDO connection","text":"<p>Migraw can receive a PDO instance directly:</p> <pre><code>'connection' =&gt; $pdo,\n</code></pre>"},{"location":"configuration/#closure-or-callable","title":"Closure or callable","text":"<p>A closure returning PDO is also supported:</p> <pre><code>'connection' =&gt; static fn (): PDO =&gt; DB::connection(),\n</code></pre> <p>As is any callable returning PDO:</p> <pre><code>'connection' =&gt; [DB::class, 'connection'],\n</code></pre> <p>This makes it possible to reuse the database connection already managed by your application or framework.</p>"},{"location":"database-support/","title":"Database Support","text":"<p>Migraw supports PDO connections for:</p> Database Driver MySQL <code>mysql</code> MariaDB <code>mysql</code> PostgreSQL <code>pgsql</code> SQLite <code>sqlite</code>"},{"location":"database-support/#feature-support","title":"Feature support","text":"Feature MySQL / MariaDB PostgreSQL SQLite Raw migrations \u2713 \u2713 \u2713 Fluent migrations \u2713 \u2713 \u2713 Population migrations \u2713 \u2713 \u2713 Dry run \u2713 \u2713 \u2713 Checksums / status / repair \u2713 \u2713 \u2713 Schema squash \u2713 Planned Planned <p>Schema squash currently targets MySQL/MariaDB. PostgreSQL and SQLite squash support can be added independently because schema dumping differs between database engines.</p>"},{"location":"database-support/#pdo-drivers","title":"PDO drivers","text":"<p>Typical PHP extensions:</p> <pre><code>pdo_mysql\npdo_pgsql\npdo_sqlite\n</code></pre>"},{"location":"database-support/#portable-migrations","title":"Portable migrations","text":"<p>When a project must run on multiple database engines, prefer SQL and fluent operations supported by all target engines.</p> <p>For projects tied to one database engine, database-specific raw SQL is fully supported.</p>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#requirements","title":"Requirements","text":"<ul> <li>PHP 8.2+</li> <li>PDO</li> <li>A supported PDO database driver</li> </ul>"},{"location":"getting-started/#installation","title":"Installation","text":"<pre><code>composer require eril/migraw\n</code></pre>"},{"location":"getting-started/#initialize","title":"Initialize","text":"<p>For MySQL/MariaDB:</p> <pre><code>php vendor/bin/migraw init\n</code></pre> <p>or:</p> <pre><code>php vendor/bin/migraw init:mysql\n</code></pre> <p>For PostgreSQL:</p> <pre><code>php vendor/bin/migraw init:pgsql\n</code></pre> <p>For SQLite:</p> <pre><code>php vendor/bin/migraw init:sqlite\n</code></pre> <p>This creates <code>migraw.php</code> and, when necessary, the migrations directory. you can also use <code>--force</code> to overwrite the current <code>migraw.php</code> file.</p>"},{"location":"getting-started/#create-your-first-migration","title":"Create your first migration","text":"<pre><code>php vendor/bin/migraw make create_users_table\n</code></pre> <p>By default Migraw generates the template configured in <code>migraw.php</code>.</p> <p>To run migrations:</p> <pre><code>php vendor/bin/migraw migrate\n</code></pre> <p>You can also use:</p> <pre><code>php vendor/bin/migraw up\n</code></pre>"},{"location":"getting-started/#check-status","title":"Check status","text":"<pre><code>php vendor/bin/migraw status\n</code></pre>"},{"location":"getting-started/#roll-back","title":"Roll back","text":"<pre><code>php vendor/bin/migraw rollback\n</code></pre> <p>or:</p> <pre><code>php vendor/bin/migraw down\n</code></pre>"},{"location":"getting-started/#preview-sql","title":"Preview SQL","text":"<pre><code>php vendor/bin/migraw migrate --dry-run\n</code></pre> <p><code>--pretend</code> is also supported.</p>"},{"location":"getting-started/#next","title":"Next","text":"<ul> <li>Configuration</li> <li>Raw migrations</li> <li>Fluent migrations</li> <li>Population migrations</li> <li>Commands</li> </ul>"},{"location":"commands/","title":"Commands","text":""},{"location":"commands/#quick-reference","title":"Quick reference","text":"Command Arguments Options Purpose <code>init</code> \u2014 <code>--force</code> Generate default config <code>init:mysql</code> \u2014 <code>--force</code> Generate MySQL defaults <code>init:pgsql</code> \u2014 <code>--force</code> Generate PostgreSQL defaults <code>init:sqlite</code> \u2014 <code>--force</code> Generate SQLite defaults <code>make</code> <code>&lt;name&gt;</code> <code>--sql</code>, <code>--populate</code> Create a migration <code>migrate</code> / <code>up</code> \u2014 <code>--dry-run</code>, <code>--pretend</code> Run pending migrations <code>rollback</code> / <code>down</code> \u2014 <code>--dry-run</code>, <code>--pretend</code> Roll back last batch <code>reset</code> \u2014 <code>--dry-run</code>, <code>--pretend</code> Roll back all migrations <code>refresh</code> \u2014 <code>--dry-run</code>, <code>--pretend</code> Roll back and rerun <code>fresh</code> \u2014 \u2014 Rebuild from a clean schema <code>status</code> \u2014 \u2014 Show migration status <code>validate</code> \u2014 \u2014 Validate migration files <code>doctor</code> \u2014 \u2014 Check environment/config <code>repair</code> \u2014 <code>--modified</code>, <code>--force</code> Repair repository records <code>squash</code> <code>[name]</code> <code>--force</code> Create a schema baseline <code>unsquash</code> <code>[archive]</code> <code>--force</code> Restore pre-squash history <code>help</code> \u2014 \u2014 Show CLI help"},{"location":"commands/#init","title":"<code>init</code>","text":"<pre><code>php vendor/bin/migraw init\n</code></pre> <p>Driver-specific defaults:</p> <pre><code>php vendor/bin/migraw init:mysql\nphp vendor/bin/migraw init:pgsql\nphp vendor/bin/migraw init:sqlite\n</code></pre> <p>Overwrite an existing generated config:</p> <pre><code>php vendor/bin/migraw init:mysql --force\n</code></pre>"},{"location":"commands/#make","title":"<code>make</code>","text":"<pre><code>php vendor/bin/migraw make create_users_table\n</code></pre>"},{"location":"commands/#argument","title":"Argument","text":"Argument Required Description <code>name</code> Yes Migration name used for template detection and filename"},{"location":"commands/#options","title":"Options","text":"Option Description <code>--sql</code> Force a blank raw SQL migration <code>--populate</code> Create a <code>PopulatorMigration</code> <p>Examples:</p> <pre><code>php vendor/bin/migraw make add_phone_to_users\n</code></pre> <pre><code>php vendor/bin/migraw make custom_database_change --sql\n</code></pre> <pre><code>php vendor/bin/migraw make populate_roles --populate\n</code></pre> <p><code>--sql</code> and <code>--populate</code> cannot be used together.</p>"},{"location":"commands/#migrate-up","title":"<code>migrate</code> / <code>up</code>","text":"<p>Run pending migrations:</p> <pre><code>php vendor/bin/migraw migrate\n</code></pre> <p>Alias:</p> <pre><code>php vendor/bin/migraw up\n</code></pre> <p>Preview without executing:</p> <pre><code>php vendor/bin/migraw migrate --dry-run\n</code></pre> <p>or:</p> <pre><code>php vendor/bin/migraw migrate --pretend\n</code></pre>"},{"location":"commands/#rollback-down","title":"<code>rollback</code> / <code>down</code>","text":"<p>Roll back the latest batch:</p> <pre><code>php vendor/bin/migraw rollback\n</code></pre> <p>Alias:</p> <pre><code>php vendor/bin/migraw down\n</code></pre> <p>Preview:</p> <pre><code>php vendor/bin/migraw rollback --dry-run\n</code></pre>"},{"location":"commands/#reset","title":"<code>reset</code>","text":"<p>Roll back all managed migrations:</p> <pre><code>php vendor/bin/migraw reset\n</code></pre> <p>Preview:</p> <pre><code>php vendor/bin/migraw reset --pretend\n</code></pre>"},{"location":"commands/#refresh","title":"<code>refresh</code>","text":"<p>Roll back all managed migrations and execute them again:</p> <pre><code>php vendor/bin/migraw refresh\n</code></pre> <p>Preview:</p> <pre><code>php vendor/bin/migraw refresh --dry-run\n</code></pre>"},{"location":"commands/#fresh","title":"<code>fresh</code>","text":"<p>Rebuild from a clean schema:</p> <pre><code>php vendor/bin/migraw fresh\n</code></pre> <p>Use this when you intentionally want a clean database rebuilt from the active migrations.</p>"},{"location":"commands/#status","title":"<code>status</code>","text":"<pre><code>php vendor/bin/migraw status\n</code></pre> <p>Typical states include:</p> <pre><code>[ran]\n[pending]\n[ran modified]\n[missing]\n</code></pre>"},{"location":"commands/#validate","title":"<code>validate</code>","text":"<pre><code>php vendor/bin/migraw validate\n</code></pre> <p>Checks migration files and verifies that migration methods return supported statement types.</p>"},{"location":"commands/#doctor","title":"<code>doctor</code>","text":"<pre><code>php vendor/bin/migraw doctor\n</code></pre> <p>Checks common setup problems such as:</p> <ul> <li>config file</li> <li>configured bootstrap</li> <li>database connection</li> <li>PDO driver</li> <li>migrations path</li> <li>migration repository</li> </ul>"},{"location":"commands/#repair","title":"<code>repair</code>","text":"<p>Remove migration records whose files are missing:</p> <pre><code>php vendor/bin/migraw repair\n</code></pre> <p>Accept current checksums of intentionally modified migrations:</p> <pre><code>php vendor/bin/migraw repair --modified\n</code></pre> <p>Skip confirmation:</p> <pre><code>php vendor/bin/migraw repair --modified --force\n</code></pre> <p><code>repair --modified</code> updates the repository checksum. It does not rerun the migration.</p>"},{"location":"commands/#squash","title":"<code>squash</code>","text":"<pre><code>php vendor/bin/migraw squash\n</code></pre> <p>Custom baseline name:</p> <pre><code>php vendor/bin/migraw squash app_schema\n</code></pre> <p>Skip confirmation:</p> <pre><code>php vendor/bin/migraw squash app_schema --force\n</code></pre> <p>See Squash &amp; Unsquash.</p>"},{"location":"commands/#unsquash","title":"<code>unsquash</code>","text":"<p>Restore the latest squash history:</p> <pre><code>php vendor/bin/migraw unsquash\n</code></pre> <p>Select an archive:</p> <pre><code>php vendor/bin/migraw unsquash 20260826_010000\n</code></pre> <p>Skip confirmation:</p> <pre><code>php vendor/bin/migraw unsquash 20260826_010000 --force\n</code></pre> <p>Unsquash restores migration history and files. It does not roll back the current database schema.</p>"},{"location":"commands/#help","title":"<code>help</code>","text":"<pre><code>php vendor/bin/migraw help\n</code></pre>"},{"location":"commands/baseline/","title":"Baseline","text":"<p>The <code>baseline</code> command lets you start using Migraw in an existing project that already has a database schema but no Migraw migration history.</p> <p>Instead of recreating the project's historical migrations, Migraw captures the current schema and uses it as the starting point for future migrations.</p>"},{"location":"commands/baseline/#create-a-baseline","title":"Create a baseline","text":"<pre><code>php vendor/bin/migraw baseline\n</code></pre> <p>This generates a migration named similar to:</p> <pre><code>2026_08_30_120000_baseline.php\n</code></pre> <p>You can provide a custom name:</p> <pre><code>php vendor/bin/migraw baseline legacy_schema\n</code></pre> <p>which generates something similar to:</p> <pre><code>2026_08_30_120000_legacy_schema.php\n</code></pre>"},{"location":"commands/baseline/#what-happens","title":"What happens","text":"<p>When a baseline is created, Migraw:</p> <ol> <li>Reads the current database schema.</li> <li>Generates a normal migration capable of recreating that schema.</li> <li>Calculates the migration checksum.</li> <li>Registers the baseline in the migration repository as already executed.</li> </ol> <p>The existing application schema and data are not modified.</p> <p>Conceptually:</p> <pre><code>Existing project\n\nDatabase\n\u251c\u2500\u2500 users\n\u251c\u2500\u2500 roles\n\u251c\u2500\u2500 posts\n\u2514\u2500\u2500 ...\n\nMigraw history\n\u2514\u2500\u2500 empty\n\n        \u2502\n        \u2502 migraw baseline\n        \u25bc\n\ndatabase/migrations/\n\u2514\u2500\u2500 2026_08_30_120000_baseline.php\n\nMigraw history\n\u2514\u2500\u2500 2026_08_30_120000_baseline [ran]\n\nDatabase\n\u251c\u2500\u2500 users\n\u251c\u2500\u2500 roles\n\u251c\u2500\u2500 posts\n\u2514\u2500\u2500 ...\n\n        unchanged\n</code></pre> <p>Migraw does not execute the generated baseline against the existing database.</p>"},{"location":"commands/baseline/#continue-with-normal-migrations","title":"Continue with normal migrations","text":"<p>After creating the baseline, use Migraw normally.</p> <p>For example:</p> <pre><code>php vendor/bin/migraw make add_timezone_to_users\nphp vendor/bin/migraw migrate\n</code></pre> <p>The migration history then becomes:</p> <pre><code>2026_08_30_120000_baseline\n2026_08_30_130000_add_timezone_to_users\n</code></pre> <p>Only the new migration is executed against the existing database.</p>"},{"location":"commands/baseline/#fresh-installations","title":"Fresh installations","text":"<p>A baseline is a normal Migraw migration.</p> <p>This means the same migration can be used to create the initial schema on a fresh database.</p> <p>For example, after cloning the project:</p> <pre><code>php vendor/bin/migraw migrate\n</code></pre> <p>Migraw executes:</p> <pre><code>baseline\n    \u2193\nlater migrations\n</code></pre> <p>The baseline recreates the schema that existed when it was generated, and subsequent migrations bring the database up to the current state.</p> <p>This makes the baseline both:</p> <ul> <li>the adoption point for the existing database;</li> <li>the starting schema for future installations.</li> </ul>"},{"location":"commands/baseline/#safety-checks","title":"Safety checks","text":"<p>Baseline creation is intentionally strict.</p> <p>Migraw refuses to create a baseline when migration history already exists.</p> <p>In that situation, the project is already managed by Migraw and <code>squash</code> should normally be used instead.</p> <p>Migraw also refuses baseline creation when migration files already exist in the configured migration directory.</p> <p>This prevents existing migration files from unexpectedly becoming pending migrations after the baseline is created.</p> <p>Finally, Migraw requires an application schema to capture.</p> <p>If no application tables are found, no baseline is created.</p>"},{"location":"commands/baseline/#existing-data","title":"Existing data","text":"<p>A baseline captures the database schema, not the existing application data.</p> <p>For example, if the existing database contains:</p> <pre><code>roles\n\n1  admin  Administrator\n2  user   User\n</code></pre> <p>the baseline recreates the <code>roles</code> table structure, but does not automatically include those records.</p> <p>Data required by new installations should be represented explicitly using population migrations.</p> <p>For example:</p> <pre><code>php vendor/bin/migraw make populate_roles --populate\n</code></pre> <p>Population migrations should be idempotent so they can safely reconcile data that may already exist in the original database.</p>"},{"location":"commands/baseline/#baseline-vs-squash","title":"Baseline vs squash","text":"<p><code>baseline</code> and <code>squash</code> both generate schema migrations, but they solve different problems.</p>"},{"location":"commands/baseline/#baseline_1","title":"Baseline","text":"<p>Use <code>baseline</code> when:</p> <pre><code>database schema exists\n+\nMigraw history does not exist\n</code></pre> <p>It establishes the first Migraw migration for an existing project.</p>"},{"location":"commands/baseline/#squash","title":"Squash","text":"<p>Use <code>squash</code> when:</p> <pre><code>database schema exists\n+\nMigraw migration history already exists\n</code></pre> <p>It consolidates an existing migration history into a new schema baseline while preserving the information required for existing installations to reconcile the squash.</p> <p>In short:</p> <pre><code>Existing project adopting Migraw\n\u2192 baseline\n\nExisting Migraw project consolidating history\n\u2192 squash\n</code></pre>"},{"location":"commands/baseline/#deployment","title":"Deployment","text":"<p>After creating a baseline, commit the generated migration to version control:</p> <pre><code>git add database/migrations\ngit commit -m \"Add initial Migraw baseline\"\n</code></pre> <p>From that point forward, the baseline is part of the project's migration history and should be deployed with the application like any other migration.</p> <p>Existing database:</p> <pre><code>baseline already marked as ran\n        \u2193\nmigraw migrate\n        \u2193\nonly later migrations execute\n</code></pre> <p>Fresh database:</p> <pre><code>empty migration history\n        \u2193\nmigraw migrate\n        \u2193\nbaseline executes\n        \u2193\nlater migrations execute\n</code></pre>"},{"location":"commands/baseline/#summary","title":"Summary","text":"<p>Use:</p> <pre><code>php vendor/bin/migraw baseline\n</code></pre> <p>when introducing Migraw to an existing project.</p> <p>The command captures the current schema, creates a reproducible migration, and marks it as already executed without modifying the existing application schema or data.</p> <p>After that, the project can use normal Migraw migrations going forward.</p>"},{"location":"commands/squash/","title":"Squash &amp; Unsquash","text":""},{"location":"commands/squash/#what-squash-does","title":"What squash does","text":"<p>Squash replaces a long migration history with a new schema baseline representing the current database schema.</p> <p>For example:</p> <pre><code>001_create_users\n002_add_phone\n003_create_roles\n004_create_consultas\n005_alter_consultas\n</code></pre> <p>becomes approximately:</p> <pre><code>006_schema\n</code></pre> <p>Old schema migrations are archived rather than discarded.</p> <p>Population migrations are preserved and retimestamped after the new baseline.</p>"},{"location":"commands/squash/#run-squash","title":"Run squash","text":"<pre><code>php vendor/bin/migraw squash\n</code></pre> <p>You can provide a baseline name:</p> <pre><code>php vendor/bin/migraw squash app_schema\n</code></pre> <p>Use <code>--force</code> to skip confirmation:</p> <pre><code>php vendor/bin/migraw squash app_schema --force\n</code></pre>"},{"location":"commands/squash/#what-happens","title":"What happens","text":"<p>When squash runs, Migraw:</p> <ol> <li>verifies that schema migrations are not pending;</li> <li>reads the current database schema;</li> <li>creates a new baseline migration;</li> <li>archives superseded schema migrations;</li> <li>retimestamps preserved population migrations;</li> <li>replaces the migration repository history with the new baseline and executed population migrations;</li> <li>stores the previous migration history and recovery information in <code>manifest.json</code>.</li> </ol> <p>The existing database schema and application data are not recreated during squash.</p>"},{"location":"commands/squash/#new-installations","title":"New installations","text":"<p>After squash, a new installation starts from the baseline instead of replaying the entire historical migration chain.</p> <pre><code>schema baseline\n      \u2193\npopulation migrations\n      \u2193\nnew migrations\n</code></pre> <p>For a new database, the baseline is a normal pending migration and its <code>up()</code> operation builds the schema.</p>"},{"location":"commands/squash/#existing-installations","title":"Existing installations","text":"<p>Existing databases must not execute the baseline over tables that already exist.</p> <p>Starting with Migraw 1.5, the normal <code>migrate</code> command automatically reconciles existing migration history with committed squash checkpoints.</p> <pre><code>php vendor/bin/migraw migrate\n</code></pre> <p>Migraw determines the appropriate migration path from the existing repository history.</p>"},{"location":"commands/squash/#already-at-the-pre-squash-state","title":"Already at the pre-squash state","text":"<p>If the database has executed the complete migration history represented by the squash:</p> <pre><code>001\n002\n003\n      \u2193\n006_schema\n</code></pre> <p>Migraw adopts <code>006_schema</code> as the new representation of that history.</p> <p>The baseline <code>up()</code> operation is not executed.</p>"},{"location":"commands/squash/#behind-the-squash-checkpoint","title":"Behind the squash checkpoint","text":"<p>An existing installation may not have executed every migration that existed when the squash was created.</p> <p>For example:</p> <pre><code>Squash history:\n001\n002\n003\n004\n      \u2193\n006_schema\n\nExisting database:\n001\n002\n</code></pre> <p>Migraw detects the missing migrations and executes them from the committed squash archive:</p> <pre><code>001\n002\n \u2193\n003  \u2190 archive\n004  \u2190 archive\n \u2193\n006_schema adopted\n</code></pre> <p>After catch-up completes, the old repository history is replaced by the baseline representation.</p> <p>The baseline itself is not executed against the existing schema.</p>"},{"location":"commands/squash/#after-adoption","title":"After adoption","text":"<p>Any migrations created after the baseline continue normally:</p> <pre><code>old history\n     \u2193\ncatch-up if required\n     \u2193\nbaseline adoption\n     \u2193\nnew migrations\n</code></pre> <p>For example:</p> <pre><code>001\n002\n003\n      \u2193\n006_schema\n007_add_timezone\n008_create_settings\n</code></pre> <p>An existing installation can safely run:</p> <pre><code>php vendor/bin/migraw migrate\n</code></pre> <p>Migraw reconciles the squash checkpoint first and then executes <code>007_add_timezone</code>, <code>008_create_settings</code>, and any other pending migrations.</p>"},{"location":"commands/squash/#pretend-mode","title":"Pretend mode","text":"<p>Use <code>--pretend</code> to preview the complete migration path without modifying the database or migration repository:</p> <pre><code>php vendor/bin/migraw migrate --pretend\n</code></pre> <p>Pretend mode also understands squash checkpoints.</p> <p>If an existing database is behind a squash, the output includes SQL for:</p> <ul> <li>missing archived migrations required for catch-up;</li> <li>pending population migrations when applicable;</li> <li>migrations created after the squash.</li> </ul> <p>It does not execute the baseline SQL when the baseline would only be adopted.</p> <p>For example:</p> <pre><code>existing database:\n001\n\nsquash checkpoint:\n001\n002\n003\n    \u2193\n006_schema\n\nnew migration:\n007_add_timezone\n</code></pre> <p>The pretend migration path is effectively:</p> <pre><code>002\n003\n007_add_timezone\n</code></pre> <p>The database and migration repository remain unchanged.</p>"},{"location":"commands/squash/#diverged-histories","title":"Diverged histories","text":"<p>Squash adoption is only performed when the existing migration history can be safely reconciled with the history stored in the squash manifest.</p> <p>Migraw refuses adoption when it detects conditions such as:</p> <ul> <li>unexpected migrations in the existing history;</li> <li>incompatible migration ordering;</li> <li>checksum mismatches;</li> <li>modified archived migrations;</li> <li>modified preserved population migrations.</li> </ul> <p>Migraw does not silently force a database into a squash checkpoint when its history cannot be verified.</p> <p>Resolve the migration history conflict before running <code>migrate</code> again.</p>"},{"location":"commands/squash/#population-migrations","title":"Population migrations","text":"<p>Population migrations are not merged into the schema baseline.</p> <p>During squash they are preserved and retimestamped so they remain ordered after the generated baseline.</p> <p>For example:</p> <pre><code>001_create_roles\n002_create_users\n003_populate_roles\n</code></pre> <p>may become:</p> <pre><code>004_schema\n005_populate_roles\n</code></pre> <p>If <code>003_populate_roles</code> had already been executed, its executed state is preserved when the baseline is adopted.</p> <p>If it was pending, it remains pending and can execute normally after the baseline.</p> <p>Migraw also tracks population migration renames across later squash checkpoints so existing installations can be reconciled through multiple squashes.</p>"},{"location":"commands/squash/#multiple-squashes","title":"Multiple squashes","text":"<p>A project may squash its migration history more than once.</p> <p>For example:</p> <pre><code>001\n002\n003\n    \u2193\n100_schema\n\n101_add_profile\n102_add_settings\n    \u2193\n200_schema\n\n201_add_timezone\n</code></pre> <p>Each completed squash archive acts as a migration history checkpoint.</p> <p>When necessary, Migraw can reconcile an existing installation through the committed checkpoints in order:</p> <pre><code>old installation\n      \u2193\nfirst checkpoint\n      \u2193\nsecond checkpoint\n      \u2193\ncurrent migrations\n</code></pre> <p>This allows long-lived installations to catch up even when multiple squashes have occurred since their last deployment.</p>"},{"location":"commands/squash/#archive","title":"Archive","text":"<p>A squash archive contains superseded schema migrations and a <code>manifest.json</code> snapshot.</p> <p>Example:</p> <pre><code>database/migrations/\n\u251c\u2500\u2500 2026_08_29_010000_schema.php\n\u251c\u2500\u2500 2026_08_29_010001_populate_roles.php\n\u251c\u2500\u2500 2026_08_29_020000_add_timezone.php\n\u2514\u2500\u2500 archive/\n    \u2514\u2500\u2500 20260829_010000/\n        \u251c\u2500\u2500 manifest.json\n        \u251c\u2500\u2500 2026_06_01_create_users.php\n        \u251c\u2500\u2500 2026_06_02_create_roles.php\n        \u2514\u2500\u2500 ...\n</code></pre> <p>The archive is not disposable backup data.</p> <p>It is part of the managed migration history and may be required to bring an existing installation forward after a squash.</p> <p>Commit the generated baseline, retimestamped population migrations, <code>archive/</code> directory, and <code>manifest.json</code> together to version control.</p> <p>Do not remove old squash archives while installations may still depend on them for migration catch-up or squash recovery.</p>"},{"location":"commands/squash/#manifestjson","title":"manifest.json","text":"<p>Each squash archive contains a <code>manifest.json</code> describing the checkpoint.</p> <p>It records information required for reconciliation and recovery, including:</p> <ul> <li>the generated baseline;</li> <li>the migration repository history before squash;</li> <li>archived schema migrations;</li> <li>preserved and retimestamped population migrations;</li> <li>migration checksums;</li> <li>squash status and timestamps.</li> </ul> <p>Migraw uses completed manifests when reconciling existing installations during <code>migrate</code>.</p> <p>The manifest should be committed together with its archive and should not be edited manually.</p>"},{"location":"commands/squash/#deployment-workflow","title":"Deployment workflow","text":"<p>A typical squash deployment is:</p> <pre><code>Development\n    \u2502\n    \u251c\u2500\u2500 run all pending migrations\n    \u2502\n    \u251c\u2500\u2500 squash\n    \u2502\n    \u251c\u2500\u2500 commit baseline\n    \u251c\u2500\u2500 commit preserved populations\n    \u251c\u2500\u2500 commit archive + manifest\n    \u2502\n    \u2514\u2500\u2500 deploy\n            \u2502\n            \u25bc\n        Production\n            \u2502\n            \u2514\u2500\u2500 migraw migrate\n                    \u2502\n                    \u251c\u2500\u2500 fresh database\n                    \u2502      \u2514\u2500\u2500 execute baseline\n                    \u2502\n                    \u251c\u2500\u2500 current old database\n                    \u2502      \u2514\u2500\u2500 adopt baseline\n                    \u2502\n                    \u251c\u2500\u2500 behind database\n                    \u2502      \u2514\u2500\u2500 catch up from archive\n                    \u2502          \u2192 adopt baseline\n                    \u2502\n                    \u2514\u2500\u2500 diverged database\n                           \u2514\u2500\u2500 abort\n</code></pre> <p>No special production squash command is required.</p> <p>The deployed application continues to use:</p> <pre><code>php vendor/bin/migraw migrate\n</code></pre>"},{"location":"commands/squash/#unsquash","title":"Unsquash","text":"<p>Unsquash restores the migration history represented by a squash archive.</p> <pre><code>php vendor/bin/migraw unsquash\n</code></pre> <p>To select a specific archive:</p> <pre><code>php vendor/bin/migraw unsquash 20260829_010000\n</code></pre> <p>Unsquash restores:</p> <ul> <li>archived schema migration files;</li> <li>original population migration filenames;</li> <li>migration batches;</li> <li>checksums;</li> <li>execution history.</li> </ul> <p>It removes the corresponding squash baseline from the active migration files.</p>"},{"location":"commands/squash/#unsquash-is-not-rollback","title":"Unsquash is not rollback","text":"<p><code>rollback</code> changes the database schema by executing migration <code>down()</code> operations.</p> <p><code>unsquash</code> changes how the existing database state is represented by migration history.</p> <p>It does not revert the current database schema or application data.</p> <p>Migraw refuses unsafe unsquash operations when the current migration history no longer matches the squash state.</p>"},{"location":"guides/common-workflows/","title":"Common Workflows","text":""},{"location":"guides/common-workflows/#add-a-column","title":"Add a column","text":"<pre><code>php vendor/bin/migraw make add_phone_to_users\n</code></pre> <p>Raw example:</p> <pre><code>return $this-&gt;raw(&lt;&lt;&lt;'SQL'\nALTER TABLE users\n    ADD COLUMN phone VARCHAR(50) NULL;\nSQL);\n</code></pre> <p>Fluent example:</p> <pre><code>return $this-&gt;alter('users')\n    -&gt;add('phone VARCHAR(50) NULL');\n</code></pre>"},{"location":"guides/common-workflows/#add-an-index","title":"Add an index","text":"<pre><code>return $this-&gt;alter('users')\n    -&gt;index('idx_users_email', 'email');\n</code></pre>"},{"location":"guides/common-workflows/#add-a-foreign-key","title":"Add a foreign key","text":"<pre><code>return $this-&gt;alter('posts')\n    -&gt;foreign(\n        columns: 'user_id',\n        referencesTable: 'users',\n        referencesColumns: 'id',\n        name: 'fk_posts_user',\n        onDelete: 'CASCADE'\n    );\n</code></pre>"},{"location":"guides/common-workflows/#preview-a-migration","title":"Preview a migration","text":"<pre><code>php vendor/bin/migraw migrate --dry-run\n</code></pre>"},{"location":"guides/common-workflows/#undo-the-latest-batch","title":"Undo the latest batch","text":"<pre><code>php vendor/bin/migraw rollback\n</code></pre>"},{"location":"guides/common-workflows/#rebuild-a-development-database","title":"Rebuild a development database","text":"<pre><code>php vendor/bin/migraw fresh\n</code></pre>"},{"location":"guides/common-workflows/#check-for-modified-migrations","title":"Check for modified migrations","text":"<pre><code>php vendor/bin/migraw status\n</code></pre> <p>If an executed migration was intentionally changed:</p> <pre><code>php vendor/bin/migraw repair --modified\n</code></pre>"},{"location":"guides/common-workflows/#compact-a-long-migration-history","title":"Compact a long migration history","text":"<pre><code>php vendor/bin/migraw migrate\nphp vendor/bin/migraw squash app_schema\n</code></pre>"},{"location":"guides/common-workflows/#restore-the-pre-squash-migration-history","title":"Restore the pre-squash migration history","text":"<pre><code>php vendor/bin/migraw unsquash\n</code></pre>"},{"location":"migrations/","title":"Migrations","text":"<p>A Migraw migration represents a database change with an <code>up()</code> and <code>down()</code> operation.</p> <pre><code>&lt;?php\n\nuse Eril\\Migraw\\Migration;\nuse Eril\\Migraw\\Sql\\SqlStatement;\n\nreturn new class extends Migration\n{\n    public function up(): string|array|SqlStatement\n    {\n        // Apply the change.\n    }\n\n    public function down(): string|array|SqlStatement\n    {\n        // Reverse the change.\n    }\n};\n</code></pre> <p>Migraw supports two main styles:</p> <ul> <li>Raw SQL \u2014 write SQL directly with <code>$this-&gt;raw()</code>.</li> <li>Fluent \u2014 use Migraw's SQL statement helpers.</li> </ul> <p>The default generated style is controlled by:</p> <pre><code>'template' =&gt; 'raw',\n</code></pre> <p>Use population migrations when the migration exists to insert stable application data rather than change the schema.</p> <p>See:</p> <ul> <li>Raw SQL</li> <li>Fluent</li> <li>Population</li> </ul>"},{"location":"migrations/fluent/","title":"Fluent Migrations","text":"<p>Migraw's fluent API keeps common schema operations concise while keeping SQL definitions visible.</p> <p>Set fluent as the default generated template:</p> <pre><code>'template' =&gt; 'fluent',\n</code></pre> <p>Then create migrations normally:</p> <pre><code>php vendor/bin/migraw make create_users_table\n</code></pre>"},{"location":"migrations/fluent/#create-a-table","title":"Create a table","text":"<pre><code>&lt;?php\n\nuse Eril\\Migraw\\Migration;\nuse Eril\\Migraw\\Sql\\SqlStatement;\n\nreturn new class extends Migration\n{\n    public function up(): SqlStatement\n    {\n        return $this-&gt;create('users')\n            -&gt;id()\n            -&gt;column('name VARCHAR(255) NOT NULL')\n            -&gt;column('email VARCHAR(180) NOT NULL')\n            -&gt;unique('uq_users_email', 'email')\n            -&gt;timestamps();\n    }\n\n    public function down(): SqlStatement\n    {\n        return $this-&gt;drop('users')-&gt;ifExists();\n    }\n};\n</code></pre>"},{"location":"migrations/fluent/#createtable-methods","title":"CreateTable methods","text":"Method Purpose <code>ifNotExists()</code> Adds <code>IF NOT EXISTS</code> <code>id($definition = 'id')</code> Adds a conventional or custom primary-key definition <code>column($definition)</code> Adds a SQL column definition <code>timestamps()</code> Adds <code>created_at</code> and <code>updated_at</code> <code>softDeletes($name = 'deleted_at')</code> Adds a nullable soft-delete timestamp <code>primary($columns, $name = null)</code> Adds a primary-key constraint <code>unique($name, $columns)</code> Adds a unique constraint <code>index($name, $columns)</code> Adds an index <code>foreign(...)</code> Adds a foreign key <code>constraint($definition)</code> Adds a table-level constraint <code>raw($definition)</code> Adds a raw table definition"},{"location":"migrations/fluent/#id","title":"<code>id()</code>","text":"<p>Default:</p> <pre><code>$this-&gt;create('users')\n    -&gt;id();\n</code></pre> <p>Custom column name:</p> <pre><code>$this-&gt;create('users')\n    -&gt;id('user_id');\n</code></pre> <p>UUID shorthand:</p> <pre><code>$this-&gt;create('users')\n    -&gt;id('uuid');\n</code></pre> <p>Custom SQL definition:</p> <pre><code>$this-&gt;create('users')\n    -&gt;id('uuid CHAR(36) PRIMARY KEY');\n</code></pre> <p>Migraw compiles supported shorthand according to the active database driver.</p>"},{"location":"migrations/fluent/#columns","title":"Columns","text":"<p>Column definitions are SQL fragments:</p> <pre><code>$this-&gt;create('products')\n    -&gt;id()\n    -&gt;column('name VARCHAR(255) NOT NULL')\n    -&gt;column('price DECIMAL(10,2) NOT NULL DEFAULT 0')\n    -&gt;column('active TINYINT(1) NOT NULL DEFAULT 1');\n</code></pre>"},{"location":"migrations/fluent/#timestamps","title":"Timestamps","text":"<pre><code>$this-&gt;create('posts')\n    -&gt;id()\n    -&gt;timestamps();\n</code></pre> <p>Adds conventional <code>created_at</code> and <code>updated_at</code> columns using driver-aware SQL.</p>"},{"location":"migrations/fluent/#soft-deletes","title":"Soft deletes","text":"<pre><code>$this-&gt;create('posts')\n    -&gt;id()\n    -&gt;softDeletes();\n</code></pre> <p>Custom name:</p> <pre><code>-&gt;softDeletes('removed_at')\n</code></pre>"},{"location":"migrations/fluent/#primary-keys","title":"Primary keys","text":"<p>Single column:</p> <pre><code>-&gt;primary('id')\n</code></pre> <p>Composite primary key:</p> <pre><code>-&gt;primary(['tenant_id', 'user_id'])\n</code></pre> <p>Named constraint:</p> <pre><code>-&gt;primary(['tenant_id', 'user_id'], 'pk_tenant_user')\n</code></pre>"},{"location":"migrations/fluent/#unique-constraints","title":"Unique constraints","text":"<pre><code>-&gt;unique('uq_users_email', 'email')\n</code></pre> <p>Composite:</p> <pre><code>-&gt;unique(\n    'uq_membership',\n    ['user_id', 'organization_id']\n)\n</code></pre>"},{"location":"migrations/fluent/#indexes","title":"Indexes","text":"<pre><code>-&gt;index('idx_users_status', 'status')\n</code></pre> <p>Composite:</p> <pre><code>-&gt;index(\n    'idx_orders_customer_status',\n    ['customer_id', 'status']\n)\n</code></pre>"},{"location":"migrations/fluent/#foreign-keys","title":"Foreign keys","text":"<p>Basic:</p> <pre><code>-&gt;foreign('role_id', 'roles')\n</code></pre> <p>Custom referenced column:</p> <pre><code>-&gt;foreign(\n    'country_code',\n    'countries',\n    'code'\n)\n</code></pre> <p>Named constraint with actions:</p> <pre><code>-&gt;foreign(\n    columns: 'user_id',\n    referencesTable: 'users',\n    referencesColumns: 'id',\n    name: 'fk_posts_user',\n    onDelete: 'CASCADE',\n    onUpdate: 'CASCADE'\n)\n</code></pre> <p>Composite foreign key:</p> <pre><code>-&gt;foreign(\n    ['tenant_id', 'user_id'],\n    'tenant_users',\n    ['tenant_id', 'user_id'],\n    'fk_orders_tenant_user'\n)\n</code></pre>"},{"location":"migrations/fluent/#custom-constraints","title":"Custom constraints","text":"<pre><code>-&gt;constraint('CHECK (price &gt;= 0)')\n</code></pre>"},{"location":"migrations/fluent/#raw-table-definition","title":"Raw table definition","text":"<p>Use <code>raw()</code> inside a fluent table builder for database-specific definitions:</p> <pre><code>$this-&gt;create('articles')\n    -&gt;id()\n    -&gt;column('title VARCHAR(255) NOT NULL')\n    -&gt;raw('FULLTEXT INDEX ft_articles_title (title)');\n</code></pre>"},{"location":"migrations/fluent/#alter-a-table","title":"Alter a table","text":"<pre><code>public function up(): SqlStatement\n{\n    return $this-&gt;alter('users')\n        -&gt;add('phone VARCHAR(50) NULL')\n        -&gt;add('active TINYINT(1) NOT NULL DEFAULT 1');\n}\n</code></pre> <p><code>column()</code> and <code>add()</code> both add a column:</p> <pre><code>$this-&gt;alter('users')\n    -&gt;column('phone VARCHAR(50) NULL');\n</code></pre> <pre><code>$this-&gt;alter('users')\n    -&gt;add('phone VARCHAR(50) NULL');\n</code></pre>"},{"location":"migrations/fluent/#altertable-methods","title":"AlterTable methods","text":"Method Purpose <code>column($definition)</code> Add a column <code>add($definition)</code> Alias-style helper for adding a column <code>modify($definition)</code> Modify an existing column <code>renameColumn($from, $to)</code> Rename a column <code>dropColumn($name)</code> Drop a column <code>index($name, $columns)</code> Add an index <code>unique($name, $columns)</code> Add a unique constraint <code>foreign(...)</code> Add a foreign key <code>dropIndex($name)</code> Drop an index <code>dropForeign($name)</code> Drop a foreign-key constraint <code>raw($operation)</code> Add a raw <code>ALTER TABLE</code> operation"},{"location":"migrations/fluent/#modify-a-column","title":"Modify a column","text":"<pre><code>$this-&gt;alter('users')\n    -&gt;modify('phone VARCHAR(80) NOT NULL');\n</code></pre>"},{"location":"migrations/fluent/#rename-a-column","title":"Rename a column","text":"<pre><code>$this-&gt;alter('users')\n    -&gt;renameColumn('name', 'full_name');\n</code></pre> <p>Rollback:</p> <pre><code>$this-&gt;alter('users')\n    -&gt;renameColumn('full_name', 'name');\n</code></pre>"},{"location":"migrations/fluent/#drop-a-column","title":"Drop a column","text":"<pre><code>$this-&gt;alter('users')\n    -&gt;dropColumn('phone');\n</code></pre>"},{"location":"migrations/fluent/#add-an-index","title":"Add an index","text":"<pre><code>$this-&gt;alter('users')\n    -&gt;index('idx_users_email', 'email');\n</code></pre> <p>Composite:</p> <pre><code>$this-&gt;alter('orders')\n    -&gt;index(\n        'idx_orders_customer_status',\n        ['customer_id', 'status']\n    );\n</code></pre>"},{"location":"migrations/fluent/#add-a-unique-constraint","title":"Add a unique constraint","text":"<pre><code>$this-&gt;alter('users')\n    -&gt;unique('uq_users_email', 'email');\n</code></pre>"},{"location":"migrations/fluent/#add-a-foreign-key","title":"Add a foreign key","text":"<pre><code>$this-&gt;alter('posts')\n    -&gt;foreign(\n        columns: 'user_id',\n        referencesTable: 'users',\n        referencesColumns: 'id',\n        name: 'fk_posts_user',\n        onDelete: 'CASCADE'\n    );\n</code></pre>"},{"location":"migrations/fluent/#drop-indexes-and-foreign-keys","title":"Drop indexes and foreign keys","text":"<pre><code>$this-&gt;alter('users')\n    -&gt;dropIndex('idx_users_email');\n</code></pre> <pre><code>$this-&gt;alter('posts')\n    -&gt;dropForeign('fk_posts_user');\n</code></pre>"},{"location":"migrations/fluent/#raw-alter-operation","title":"Raw alter operation","text":"<pre><code>$this-&gt;alter('products')\n    -&gt;raw('ADD CHECK (price &gt;= 0)');\n</code></pre>"},{"location":"migrations/fluent/#drop-a-table","title":"Drop a table","text":"<pre><code>$this-&gt;drop('users');\n</code></pre> <p>Safer rollback-style usage:</p> <pre><code>$this-&gt;drop('users')-&gt;ifExists();\n</code></pre>"},{"location":"migrations/fluent/#rename-a-table","title":"Rename a table","text":"<pre><code>$this-&gt;rename('users')\n    -&gt;to('members');\n</code></pre> <p>Rollback:</p> <pre><code>$this-&gt;rename('members')\n    -&gt;to('users');\n</code></pre>"},{"location":"migrations/fluent/#multiple-fluent-statements","title":"Multiple fluent statements","text":"<p>Return an array:</p> <pre><code>public function up(): array\n{\n    return [\n        $this-&gt;create('roles')\n            -&gt;id()\n            -&gt;column('name VARCHAR(100) NOT NULL UNIQUE'),\n\n        $this-&gt;create('users')\n            -&gt;id()\n            -&gt;column('role_id INT NOT NULL')\n            -&gt;column('name VARCHAR(255) NOT NULL')\n            -&gt;foreign('role_id', 'roles'),\n    ];\n}\n</code></pre> <p>Rollback in reverse dependency order:</p> <pre><code>public function down(): array\n{\n    return [\n        $this-&gt;drop('users')-&gt;ifExists(),\n        $this-&gt;drop('roles')-&gt;ifExists(),\n    ];\n}\n</code></pre>"},{"location":"migrations/fluent/#mix-fluent-and-raw","title":"Mix fluent and raw","text":"<p>You can mix both styles:</p> <pre><code>public function up(): array\n{\n    return [\n        $this-&gt;alter('users')\n            -&gt;add('search_name VARCHAR(255) NULL'),\n\n        $this-&gt;raw(&lt;&lt;&lt;'SQL'\nCREATE INDEX idx_users_search_name\nON users (search_name);\nSQL),\n    ];\n}\n</code></pre> <p>Use fluent helpers for supported common operations and raw SQL when the database-specific statement is clearer.</p>"},{"location":"migrations/population/","title":"Population Migrations","text":"<p>Population migrations insert deterministic data required by the application.</p> <p>Common examples:</p> <ul> <li>roles</li> <li>permissions</li> <li>countries or islands</li> <li>fixed categories</li> <li>default statuses</li> <li>system configuration</li> </ul> <p>Create one with:</p> <pre><code>php vendor/bin/migraw make populate_roles --populate\n</code></pre> <p>A population migration extends <code>PopulatorMigration</code> and implements <code>population()</code>.</p> <pre><code>&lt;?php\n\nuse Eril\\Migraw\\PopulatorMigration;\nuse Eril\\Migraw\\Sql\\SqlStatement;\n\nreturn new class extends PopulatorMigration\n{\n    public function population(): string|array|SqlStatement\n    {\n        return $this-&gt;populate(\n            'roles',\n            [\n                [\n                    'slug' =&gt; 'admin',\n                    'name' =&gt; 'Administrator',\n                ],\n                [\n                    'slug' =&gt; 'user',\n                    'name' =&gt; 'User',\n                ],\n            ],\n            uniqueBy: 'slug'\n        );\n    }\n};\n</code></pre>"},{"location":"migrations/population/#arguments","title":"Arguments","text":"<p><code>populate()</code> accepts:</p> <pre><code>$this-&gt;populate(\n    string $table,\n    array $rows,\n    string|array $uniqueBy,\n    array $updateColumns = []\n)\n</code></pre> Argument Description <code>$table</code> Target table <code>$rows</code> Rows to insert <code>$uniqueBy</code> Column(s) identifying an existing row <code>$updateColumns</code> Optional columns updated on conflict <p>The columns used by <code>uniqueBy</code> must correspond to a <code>PRIMARY KEY</code> or <code>UNIQUE</code> constraint.</p>"},{"location":"migrations/population/#preserve-existing-rows","title":"Preserve existing rows","text":"<p>With no update columns, conflicts preserve the existing row:</p> <pre><code>return $this-&gt;populate(\n    'roles',\n    $rows,\n    uniqueBy: 'slug'\n);\n</code></pre>"},{"location":"migrations/population/#update-existing-rows","title":"Update existing rows","text":"<p>Pass update columns directly:</p> <pre><code>return $this-&gt;populate(\n    'roles',\n    [\n        [\n            'slug' =&gt; 'admin',\n            'name' =&gt; 'System Administrator',\n            'active' =&gt; true,\n        ],\n    ],\n    uniqueBy: 'slug',\n    updateColumns: [\n        'name',\n        'active',\n    ]\n);\n</code></pre> <p>Or use <code>update()</code>:</p> <pre><code>return $this-&gt;populate(\n    'roles',\n    $rows,\n    uniqueBy: 'slug'\n)-&gt;update([\n    'name',\n    'active',\n]);\n</code></pre> <p>A single column is also valid:</p> <pre><code>-&gt;update('name')\n</code></pre>"},{"location":"migrations/population/#composite-conflict-keys","title":"Composite conflict keys","text":"<pre><code>return $this-&gt;populate(\n    'permissions',\n    $rows,\n    uniqueBy: [\n        'resource',\n        'action',\n    ]\n);\n</code></pre>"},{"location":"migrations/population/#multiple-population-statements","title":"Multiple population statements","text":"<pre><code>public function population(): array\n{\n    return [\n        $this-&gt;populate(\n            'roles',\n            $roles,\n            uniqueBy: 'slug'\n        ),\n\n        $this-&gt;populate(\n            'permissions',\n            $permissions,\n            uniqueBy: ['resource', 'action']\n        ),\n    ];\n}\n</code></pre>"},{"location":"migrations/population/#rollback-behavior","title":"Rollback behavior","text":"<p><code>PopulatorMigration</code> does not remove populated data during normal rollback.</p> <p>Its default <code>down()</code> is empty. Rolling it back removes the migration record so the population can run again later.</p>"},{"location":"migrations/population/#driver-support","title":"Driver support","text":"<p>Population statements currently compile for:</p> <ul> <li>MySQL / MariaDB</li> <li>PostgreSQL</li> <li>SQLite</li> </ul>"},{"location":"migrations/raw/","title":"Raw SQL Migrations","text":"<p>Raw migrations keep SQL completely explicit.</p> <p>Set raw as the default template:</p> <pre><code>'template' =&gt; 'raw',\n</code></pre>"},{"location":"migrations/raw/#create-table","title":"Create table","text":"<pre><code>&lt;?php\n\nuse Eril\\Migraw\\Migration;\nuse Eril\\Migraw\\Sql\\SqlStatement;\n\nreturn new class extends Migration\n{\n    public function up(): string|array|SqlStatement\n    {\n        return $this-&gt;raw(&lt;&lt;&lt;'SQL'\n        CREATE TABLE users (\n            id INT NOT NULL AUTO_INCREMENT,\n            name VARCHAR(255) NOT NULL,\n            email VARCHAR(180) NOT NULL,\n            PRIMARY KEY (id),\n            UNIQUE KEY uq_users_email (email)\n        );\n        SQL);\n    }\n\n    public function down(): string|array|SqlStatement\n    {\n        return $this-&gt;raw(&lt;&lt;&lt;'SQL'\n        DROP TABLE IF EXISTS users;\n        SQL);\n    }\n};\n</code></pre>"},{"location":"migrations/raw/#alter-table","title":"Alter table","text":"<pre><code>public function up(): string|array|SqlStatement\n{\n    return $this-&gt;raw(&lt;&lt;&lt;'SQL'\n    ALTER TABLE users\n        ADD COLUMN phone VARCHAR(50) NULL;\n    SQL);\n}\n</code></pre> <p>Rollback:</p> <pre><code>public function down(): string|array|SqlStatement\n{\n    return $this-&gt;raw(&lt;&lt;&lt;'SQL'\n    ALTER TABLE users\n        DROP COLUMN phone;\n    SQL);\n}\n</code></pre>"},{"location":"migrations/raw/#multiple-statements","title":"Multiple statements","text":"<p>Return an array when you want Migraw to execute multiple independent statements in order:</p> <pre><code>public function up(): array\n{\n    return [\n        $this-&gt;raw(&lt;&lt;&lt;'SQL'\n        ALTER TABLE users\n            ADD COLUMN active TINYINT(1) NOT NULL DEFAULT 1;\n        SQL),\n\n        $this-&gt;raw(&lt;&lt;&lt;'SQL'\n        CREATE INDEX idx_users_active\n        ON users (active);\n        SQL),\n    ];\n}\n</code></pre> <p>Rollback in reverse order:</p> <pre><code>public function down(): array\n{\n    return [\n        $this-&gt;raw(&lt;&lt;&lt;'SQL'\n        DROP INDEX idx_users_active ON users;\n        SQL),\n\n        $this-&gt;raw(&lt;&lt;&lt;'SQL'\n        ALTER TABLE users\n            DROP COLUMN active;\n        SQL),\n    ];\n}\n</code></pre>"},{"location":"migrations/raw/#database-specific-sql","title":"Database-specific SQL","text":"<p>Raw migrations are useful for statements specific to the target database:</p> <pre><code>return $this-&gt;raw(&lt;&lt;&lt;'SQL'\nCREATE FULLTEXT INDEX ft_articles_body\nON articles (body);\nSQL);\n</code></pre> <p>You can use triggers, procedures, advanced constraints, expressions, indexes, and other SQL features supported by your database.</p>"},{"location":"migrations/raw/#force-a-blank-raw-migration","title":"Force a blank raw migration","text":"<p>Even if the configured default template is fluent:</p> <pre><code>php vendor/bin/migraw make custom_change --sql\n</code></pre> <p>Use raw when direct SQL is the clearest representation of the database change.</p>"}]}